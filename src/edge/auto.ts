// Edge stub for `@inariwatch/capture/auto` — side-effect-free no-op so
// `import "@inariwatch/capture/auto"` from instrumentation.ts on Edge
// runtime doesn't try to bundle the Node-only init() chain. See ./noop.ts.
export {}
