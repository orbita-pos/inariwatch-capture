"""Framework integrations.

Each submodule is optional — importing it only makes sense when you
have the corresponding framework installed. We deliberately keep the
core ``inariwatch_capture`` package framework-free so a minimal install
stays small.

>>> from inariwatch_capture.integrations.fastapi import InariWatchMiddleware
"""
